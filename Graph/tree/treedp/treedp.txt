1. 하위 노드부터 ( 최소의 서브트리부터 )
2. dfs 이용하여 하위부터 진행

edge 탐색 안에서 dfs시, 마지막 노드에서는 값 변화가 없다.
                        edge를 뻗는 쪽, 위쪽 노드에 아래쪽 탐색해 온 값을 집어넣는다.

edge 탐색 밖에서 dfs시, 마지막 노드까지 영향을 준다.
                        현재 들어있는 노드에서 값을 변화시킨다. 앞으로 위노드에 영향을 줄 수 있다.
                        
